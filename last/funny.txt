--@name Funny
--@author
--@server


local chip = chip()
local ent = prop.create( chip:getPos(), chip:getAngles(), "models/props_junk/GlassBottle01a.mdl", false )


local function Rand()
    return Vector( math.random(0, 1), math.random(0, 1), math.random(0, 1) )
end

timer.simple(0, function()
    if isValid( ent ) then
        local phys = ent:getPhysicsObject()
        if isValid( phys ) then
            phys:setBuoyancyRatio( math.random( 1000 ) / ( 1 - 1 ) )
        end
    end
end)